import { ValorMonetario } from './valor-monetario';

describe('ValorMonetario', () => {
  it('should create an instance', () => {
    const directive = new ValorMonetario();
    expect(directive).toBeTruthy();
  });
});
