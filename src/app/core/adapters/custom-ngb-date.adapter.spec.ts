import { CustomNgbDateAdapter } from './custom-ngb-date.adapter';

describe('CustomNgbDateAdapter', () => {
  it('should create an instance', () => {
    expect(new CustomNgbDateAdapter()).toBeTruthy();
  });
});
